{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @fh6/types build && pnpm --filter backend prisma:generate && pnpm --filter backend build && pnpm --filter frontend build",
  "outputDirectory": "apps/frontend/dist",
  "functions": {
    "apps/backend/api/index.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/apps/backend/api/index.js"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
